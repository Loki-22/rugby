services:
  rugby-app:
    build: .
    ports:
      - "8082:3000"
    volumes:
      - rugby_data:/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - FIXTURE_YEAR=2026
      - ENABLE_MOCK_SCORES=false
    restart: unless-stopped

volumes:
  rugby_data:
    driver: local
